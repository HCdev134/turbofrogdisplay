var funDay = "funday"
var days = ["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday"];
var playList = []; 

function holdCasts (casts,) {

}
// Global Variables 

// global for loop for (const iterator of object) 
    

    for(var i = 0; i < 8; i++){
        console.log(days[i]);
    }
    function useDays(days){
        for(var i = 0; i < 7; i++){
            console.log(days[i]);
            return days; 
        }
    }
    // append to HTML -- DHTML 

    function dh(days){
        console.log(days[i]); 
    }
// Bonus: captitalize each letter .forEach() //#endregion

// console.log(days.forEach([i]));